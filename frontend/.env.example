# Frontend (Vite) env vars — must be prefixed VITE_ to be exposed to the app.
# Copy to .env.local (gitignored) for local dev.

# Auto-login as the dev user (admin@admin.com), skipping the login screen.
# LOCAL SMOKE-TESTING ONLY — never set this in a production build.
VITE_TEST_USER=true
